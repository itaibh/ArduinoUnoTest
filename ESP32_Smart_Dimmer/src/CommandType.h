#ifndef COMMAND_TYPE_H
#define COMMAND_TYPE_H
// Define command types for clarity
enum CommandType
{
    CMD_NONE,
    CMD_LIGHT_ON_OFF,
    CMD_LIGHT_INTENSITY,
    CMD_LIGHT_WARMNESS,
    CMD_RGB,
    CMD_FAN_SPEED
};

#endif
